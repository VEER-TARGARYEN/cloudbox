# React Native / Expo dependency trees frequently hit strict npm peer-dependency
# checks over harmless patch-level mismatches (e.g. react-dom wanting a slightly
# newer react than the SDK pins, even though we never ship web). Accept Expo's
# curated versions so `npm install` works out of the box.
legacy-peer-deps=true
